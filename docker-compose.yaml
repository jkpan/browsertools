version: 'version: 2'  # Use version 2 of the Docker Compose file format
services:
  app:
    image: myapp:latest
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    volumes:
      - ./app:/usr/src/app
    networks:
      - mynetwork